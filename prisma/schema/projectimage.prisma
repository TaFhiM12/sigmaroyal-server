model ProjectImage {
  id        String   @id @default(cuid())
  url       String
  caption   String?
  project   Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  projectId String
}
